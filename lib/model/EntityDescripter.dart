/// временное решение для демо, данная сущность не должна существовать в отрыве от апи получения данных по ней самой
/// сейчас же необходимые данные заколачиваются константами
class EntityDesripter{
  final int id;
  final String name;

  const EntityDesripter(this.id, this.name);
}